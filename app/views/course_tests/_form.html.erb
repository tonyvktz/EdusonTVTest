<%= form_with(model: course_test, local: true) do |form| %>
  <% if course_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_test.errors.count, "error") %> prohibited this course_test from being saved:</h2>

      <ul>
      <% course_test.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :course_test_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
